---
alwaysApply: true
---
# React & Inertia Best Practices (Taskflow)

- React: https://react.dev
- Inertia.js: https://inertiajs.com

## Conventions

- **Components**: Functional components dengan hooks. Hindari class components.
- **Routing**: Gunakan `@/routes` (Wayfinder) untuk link dan redirect. Tidak pakai React Router.
- **Data**: Props dari Inertia via `usePage().props` atau page props.
- **Styling**: Tailwind CSS. Gunakan `clsx` atau `cva` untuk conditional classes.

## Inertia Patterns

- `Link` dari `@inertiajs/react` untuk navigasi (bukan `<a>`).
- `Head` untuk title dan meta.
- `router.visit()` untuk programmatic navigation.

## Taskflow UI/UX Prinsip

- **Feedback**: Loading state (spinner/skeleton) saat fetch, toast/alert untuk sukses/error.
- **Empty State**: Tampilkan ilustrasi + CTA jelas saat belum ada task.
- **Hierarchy**: Prioritas visual jelas (urgent > high > medium > low). Gunakan warna/badge konsisten.
- **Accessibility**: Label form, focus states, keyboard navigation. Gunakan `aria-*` bila perlu.
- **Responsive**: Mobile-first. Task list/card harus nyaman di layar kecil.
- **Optimistic UI**: Untuk toggle complete, drag-drop—update UI dulu, sync ke server di background.

## Contoh

```tsx
import { Head, Link, usePage } from '@inertiajs/react';
import { dashboard } from '@/routes';

export default function Welcome() {
    const { auth } = usePage().props;

    return (
        <>
            <Head title="Welcome" />
            <Link href={dashboard()}>Dashboard</Link>
        </>
    );
}
```
